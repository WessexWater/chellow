. ${CHELLOW_HOME}/venv/bin/activate
cd ${CHELLOW_HOME}
python application.py &
echo $! > ${CHELLOW_HOME}/flask.pid

until curl http://127.0.0.1:5000/health --max-time 30
do
  echo "Testing if server is up..."
  sleep 1
done


