<!DOCTYPE html>
<html>
  <head>
    <link rel="stylesheet" type="text/css"
      href="{{ context_path }}/reports/19/output/">
    <title>Chellow &gt; CSV Supplies HH Duration</title>
  </head>
  <body>
    <p>
      <a href="{{context_path}}/reports/1/output/">Chellow</a> &gt;
      CSV Supplies HH Duration
    </p>
    {% if messages %}
      <ul>
        {% for message in messages %}
          <li>{{ message }}</li>
        {% endfor %}
      </ul>
    {% endif %}
    <br>
    <form action="{{context_path}}/reports/169/output/" method='post'>
      <fieldset>
        <legend>48 Column Format</legend>
        <br/>
        <select name="imp_related">
					{{input_option('imp_related', 'true', 'Import')}}
					{{input_option('imp_related', 'false', 'Export')}}
        </select>
        <select name="channel_type">
					{{input_option('channel_type', 'ACTIVE', 'ACTIVE')}}
					{{input_option('channel_type', 'REACTIVE_IMP', 'REACTIVE_IMP')}}
					{{input_option('channel_type', 'REACTIVE_EXP', 'REACTIVE_EXP')}}
        </select> from {{input_date('start', start_date)}} to
        {{input_date('finish', finish_date)}}
        <label>Zipped?</label> {{input_checkbox('is_zipped', False)}} <br><br>
        MPAN Cores (one on each line, includes all supplies if left
        blank)<br>
        {{input_textarea('mpan_cores', '', 5, 40)}} <br><br>
        <input type="submit" value="Download">
      </fieldset>
    </form>
    <br><br>
    <form action="{{context_path}}/reports/187/output/" method='post'>
      <fieldset>
        <legend>HH Per Row Format</legend>
        <br>
        <label>From</label> {{input_date('start', start_date)}}
        <label>to</label> {{input_date('finish', finish_date)}}
        <label>Zipped?</label> {{input_checkbox('is_zipped', False)}} <br><br>
        MPAN Cores (one on each line, includes all supplies if left
        blank)<br>
        {{input_textarea('mpan_cores', '', 5, 40)}} <br><br>
        <input type="submit" value="Download">
      </fieldset>
    </form>
  </body>
</html>

