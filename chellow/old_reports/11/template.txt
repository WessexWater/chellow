<!DOCTYPE html>
<html>
  <head>
    <link rel="stylesheet" type="text/css"
      href="{{ context_path }}/reports/19/output/" />
    <title>Chellow &gt; Sites &gt; {{site.name}}</title>
  </head>
  <body>
    <p>
      <a href="{{context_path}}/reports/1/output/">Chellow</a> &gt;
      <a href="{{context_path}}/reports/3/output/">Sites</a> &gt;
      <a href="{{context_path}}/reports/5/output/?site_id={{site.id}}">{{site.name}}</a>
      &gt; Generation Graph
    </p>

    {% if messages %}
      <ul>
        {% for message in messages %}
          <li>{{ message }}</li>
        {% endfor %}
      </ul>
    {% endif %}

    <p>
      <img src="{{context_path}}/reports/23/output/?site_id={{site.id}}&amp;finish_year={{year}}&amp;finish_month={{month}}&amp;months={{months}}" >
    </p>
    
    <form action=".">
      <fieldset>
        <legend>Show graph</legend>
        <input type="hidden" name="site_id" value="{{site.id}}">

        For <input name="months" maxlength="2" size="2" value="{{months}}">
        months finishing at the end of
        {{input_date('finish', finish_date, 'month')}}
        <input type="submit" value="Show">
      </fieldset>
    </form>
  </body>
</html>


