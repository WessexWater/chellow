<!--
     General purpose build script for web applications and web services,
     including enhanced support for deploying directly to a Tomcat 5
     based server.

     This build script assumes that the source code of your web application
     is organized into the following subdirectories underneath the source
     code directory from which you execute the build script:

        docs                 Static documentation files to be copied to
                             the "docs" subdirectory of your distribution.

        src                  Java source code (and associated resource files)
                             to be compiled to the "WEB-INF/classes"
                             subdirectory of your web applicaiton.

        web                  Static HTML, JSP, and other content (such as
                             image files), including the WEB-INF subdirectory
                             and its configuration file contents.

-->


<!-- A "project" describes a set of targets that may be requested
     when Ant is executed.  The "default" attribute defines the
     target which is executed if no specific target is requested,
     and the "basedir" attribute defines the current working directory
     from which Ant executes the requested task.  This is normally
     set to the current working directory.
-->

<project name="Chellow" default="compile" basedir=".">

	<!-- ===================== Property Definitions =========================== -->

	<!--

  Each of the following properties are used in the build script.
  Values for these properties are set by the first place they are
  defined, from the following list:

  * Definitions on the "ant" command line (ant -Dfoo=bar compile).

  * Definitions from a "build.properties" file in the top level
    source directory of this application.

  * Definitions from a "build.properties" file in the developer's
    home directory.

  * Default definitions in this build.xml file.

  You will note below that property values can be composed based on the
  contents of previously defined properties.  This is a powerful technique
  that helps you minimize the number of changes required when your development
  environment is modified.  Note that property composition is allowed within
  "build.properties" files as well as in the "build.xml" script.

-->

	<property file="build.properties" />
	<property file="${user.home}/build.properties" />

	<!-- ==================== File and Directory Names ======================== -->


	<!--

  These properties generally define file and directory names (or paths) that
  affect where the build process stores its outputs.

  app.name             Base name of this application, used to
                       construct filenames and directories.
                       Defaults to "myapp".

  app.path             Context path to which this application should be
                       deployed (defaults to "/" plus the value of the
                       "app.name" property).

  app.version          Version number of this iteration of the application.

  build.home           The directory into which the "prepare" and
                       "compile" targets will generate their output.
                       Defaults to "build".

  catalina.home        The directory in which you have installed
                       a binary distribution of Tomcat 5.  This will
                       be used by the "deploy" target.

  dist.home            The name of the base directory in which
                       distribution files are created.
                       Defaults to "dist".

  manager.password     The login password of a user that is assigned the
                       "manager" role (so that he or she can execute
                       commands via the "/manager" web application)

  manager.url          The URL of the "/manager" web application on the
                       Tomcat installation to which we will deploy web
                       applications and web services.

  manager.username     The login username of a user that is assigned the
                       "manager" role (so that he or she can execute
                       commands via the "/manager" web application)

  javalib.home		   Where the external java libraries are kept.
-->

	<property name="app.name" value="chellow" />
	<property name="app.path" value="/${app.name}" />
	<property name="app.version" value="0.1-dev" />
	<property name="build.home" value="${basedir}/build" />
	<!-- <property name="catalina.home" value="${javalib.home}/apache-tomcat" /> -->
	<!-- <property name="catalina.home" value="C:\Program Files\Apache Software Foundation\Tomcat 6.0" /> -->
	<property name="dist.home" value="${basedir}/dist" />
	<property name="docs.home" value="${basedir}/docs" />
	<!-- <property name="manager.url" value="http://localhost:8080/manager" /> -->
	<property name="src.home" value="${basedir}/src" />
	<property name="web.home" value="${basedir}/web" />
	<property name="javalib.home" value="${user.home}/java" />
	<!-- Requires Imprimatur from http://imprimatur.sourceforge.net/ -->

	<property name="imprimatur.jar" value="${javalib.home}\imprimatur-016\imprimatur.jar" />
	<property name="test.home" value="${basedir}/test" />

	<!-- ================== Custom Ant Task Definitions ======================= -->


	<!--

  These properties define custom tasks for the Ant build tool that interact
  with the "/manager" web application installed with Tomcat 5.  Before they
  can be successfully utilized, you must perform the following steps:

  - Copy the file "server/lib/catalina-ant.jar" from your Tomcat 5
    installation into the "lib" directory of your Ant installation.

  - Create a "build.properties" file in your application's top-level
    source directory (or your user login home directory) that defines
    appropriate values for the "manager.password", "manager.url", and
    "manager.username" properties described above.

  For more information about the Manager web application, and the functionality
  of these tasks, see <http://localhost:8080/tomcat-docs/manager-howto.html>.

-->

	<taskdef name="deploy" classname="org.apache.catalina.ant.DeployTask" />
	<taskdef name="list" classname="org.apache.catalina.ant.ListTask" />
	<taskdef name="reload" classname="org.apache.catalina.ant.ReloadTask" />
	<taskdef name="undeploy" classname="org.apache.catalina.ant.UndeployTask" />



	<!--  ==================== Compilation Control Options ==================== -->

	<!--

  These properties control option settings on the Javac compiler when it
  is invoked using the <javac> task.

  compile.debug        Should compilation include the debug option?

  compile.deprecation  Should compilation include the deprecation option?

  compile.optimize     Should compilation include the optimize option?

-->

	<property name="compile.debug" value="true" />
	<property name="compile.deprecation" value="true" />
	<property name="compile.optimize" value="true" />



	<!-- ==================== External Dependencies =========================== -->


	<!--

  Use property values to define the locations of external JAR files on which
  your application will depend.  In general, these values will be used for
  two purposes:
  * Inclusion on the classpath that is passed to the Javac compiler
  * Being copied into the "/WEB-INF/lib" directory during execution
    of the "deploy" target.

  Because we will automatically include all of the Java classes that Tomcat 5
  exposes to web applications, we will not need to explicitly list any of those
  dependencies.  You only need to worry about external dependencies for JAR
  files that you are going to include inside your "/WEB-INF/lib" directory.

-->

	<!-- Dummy external dependency -->
	<!--
  <property name="foo.jar"
           value="/path/to/foo.jar"/>
-->


	<!-- ==================== Compilation Classpath =========================== -->

	<!--

  Rather than relying on the CLASSPATH environment variable, Ant includes
  features that makes it easy to dynamically construct the classpath you
  need for each compilation.  The example below constructs the compile
  classpath to include the servlet.jar file, as well as the other components
  that Tomcat makes available to web applications automatically, plus anything
  that you explicitly added.

-->

	<path id="compile.classpath">

		<!-- Include all JAR files that will be included in /WEB-INF/lib -->
		<!-- *** CUSTOMIZE HERE AS REQUIRED BY YOUR APPLICATION *** -->
		<fileset dir="${build.home}/WEB-INF/lib">
			<include name="*.jar" />
		</fileset>

		<!-- Include all elements that Tomcat exposes to applications -->
		<!--
		<pathelement location="${catalina.home}/common/classes" />
		
		<fileset dir="${catalina.home}/lib">
			<include name="*.jar" />
		</fileset>
		

		<fileset dir="${catalina.home}/common/endorsed">
			<include name="*.jar" />
		</fileset>


		<fileset dir="${catalina.home}/common/lib">
			<include name="*.jar" />
		</fileset>


		<pathelement location="${catalina.home}/shared/classes" />
		<fileset dir="${catalina.home}/shared/lib">
			<include name="*.jar" />
		</fileset>

-->
		<!-- <pathelement location="${catalina.home}/shared/classes" /> -->
		<pathelement location="${catalina.home}/classes" />
		<fileset dir="${catalina.home}">
			<include name="lib/*.jar" />
			<include name="common/endorsed/*.jar" />
			<!-- <include name="common/lib/*.jar" />
			<include name="shared/lib/*.jar" /> -->
		</fileset>
	</path>



	<!-- ==================== All Target ====================================== -->

	<!--

  The "all" target is a shortcut for running the "clean" target followed
  by the "compile" target, to force a complete recompile.

-->

	<target name="all" depends="clean,compile" description="Clean build and dist directories, then compile" />



	<!-- ==================== Clean Target ==================================== -->

	<!--

  The "clean" target deletes any previous "build" and "dist" directory,
  so that you can be ensured the application can be built from scratch.

-->

	<target name="clean" description="Delete old build and dist directories">
		<delete dir="${build.home}" />
		<delete dir="${dist.home}" />
	</target>



	<!-- ==================== Compile Target ================================== -->

	<!--

  The "compile" target transforms source files (from your "src" directory)
  into object files in the appropriate location in the build directory.
  This example assumes that you will be including your classes in an
  unpacked directory hierarchy under "/WEB-INF/classes".

-->

	<target name="compile" depends="prepare" description="Compile Java sources">

		<!-- Compile Java classes as necessary -->
		<mkdir dir="${build.home}/WEB-INF/classes" />
		<javac source="1.6" target="1.6" srcdir="${src.home}" destdir="${build.home}/WEB-INF/classes" debug="${compile.debug}" deprecation="${compile.deprecation}" optimize="${compile.optimize}">
			<classpath refid="compile.classpath" />
		</javac>

		<!-- Create monad lib -->
		<jar jarfile="${build.home}/WEB-INF/lib/monad.jar" basedir="${build.home}/WEB-INF/classes" includes="net/sf/chellow/monad/**/*" />

		<!-- Copy application resources -->
		<copy todir="${build.home}/WEB-INF/classes">
			<fileset dir="${src.home}" excludes="**/*.java" />
		</copy>

	</target>



	<!-- ==================== Dist Target ===================================== -->


	<!--

  The "dist" target creates a binary distribution of your application
  in a directory structure ready to be archived in a tar.gz or zip file.
  Note that this target depends on two others:

  * "compile" so that the entire web application (including external
    dependencies) will have been assembled

  * "javadoc" so that the application Javadocs will have been created

-->

	<target name="dist" depends="compile" description="Create binary distribution">
		<mkdir dir="${dist.home}" />
		<!-- Copy documentation subdirectories -->
		<!--
		<mkdir dir="${dist.home}/docs" />
		<copy todir="${dist.home}/docs">
			<fileset dir="${docs.home}" />
		</copy>
-->
		<!-- Create application JAR file -->
		<jar jarfile="${dist.home}/${app.name}.war" basedir="${build.home}" excludes="net/sf/chellow/monad/**/*" />

		<!-- Copy additional files to ${dist.home} as necessary -->
		<copy todir="${dist.home}" file="${basedir}/readme.txt" />
		<copy todir="${dist.home}" file="${basedir}/changelog.txt" />
	</target>



	<!-- ==================== Install Target ================================== -->

	<!--

  The "install" target tells the specified Tomcat 5 installation to dynamically
  install this web application and make it available for execution.  It does
  *not* cause the existence of this web application to be remembered across
  Tomcat restarts; if you restart the server, you will need to re-install all
  this web application.

  If you have already installed this application, and simply want Tomcat to
  recognize that you have updated Java classes (or the web.xml file), use the
  "reload" target instead.

  NOTE:  This target will only succeed if it is run from the same server that
  Tomcat is running on.

  NOTE:  This is the logical opposite of the "remove" target.

-->

	<target name="install" depends="dist" description="Install application to servlet container">

		<!-- <deploy url="${manager.url}" username="${manager.username}" password="${manager.password}" path="${app.path}" localWar="file://${build.home}" /> -->
		<deploy url="${manager.url}" username="${manager.username}" password="${manager.password}" path="${app.path}" war="file://${dist.home}/chellow.war" />
	</target>
	<!--
	<target name="one-test">
		<antcall target="performTest">
			<param name="test.id" value="05" />
		</antcall>
	</target>
-->
	<target name="reset-db" depends="compile">
		<exec osfamily="windows" executable="C:\Program Files\PostgreSQL\8.3\bin\dropdb">
			<arg line="-U postgres chellow" />
		</exec>
		<exec osfamily="windows" executable="C:\Program Files\PostgreSQL\8.3\bin\createdb" failonerror="true">
			<arg line="-U postgres -T template0 chellow" />
		</exec>

		<exec osfamily="unix" executable="dropdb">
			<arg line="-U postgres chellow" />
		</exec>
		<exec osfamily="unix" executable="createdb" failonerror="true">
			<arg line="-U postgres -T template0 chellow" />
		</exec>

		<!--
		<exec executable="C:\Program Files\PostgreSQL\8.1\bin\pg_restore" failonerror="true">
								<arg line='-U postgres "C:\Documents and Settings\Tony Locke\My Documents\chellow_back\chellow_back.pgdump" -d chellow' />
						</exec>
-->
	</target>

	<!--
	<target name="reset-db" depends="compile">
		<exec executable="dropdb">
			<arg line="chellow" />
		</exec>
		<exec executable="createdb" failonerror="true">
			<arg line="-T template0 chellow" />
		</exec>
		<exec executable="psql" failonerror="true">
			<arg line='-f /home/tlocke/temp/chellow.pgdump2006-08-18 chellow' />
		</exec>
	</target>
-->
	<target name="setup-test" depends="compile" description="Tests the application">
		<antcall target="reset-db" />
		<antcall target="install" />
		<!--
		<copy file="${test.home}/imprimatur.pre.xml" tofile="${test.home}/imprimatur.xml" />
		<replace file="${test.home}/imprimatur.xml" token="@@test.port@@" value="${test.port}" />
		<java jar="${imprimatur.jar}" fork="true" failonerror="true">
			<arg value="${test.home}/imprimatur.xml" />
		</java>
		-->
		<!--
		<java jar="${imprimatur.jar}" fork="true" failonerror="true">
					<arg value="${test.home}/${test.id}/imprimatur.xml" />
				</java>
		<antcall target="performTest">
			<param name="test.id" value="01" />
		</antcall>
		<antcall target="performTest">
			<param name="test.id" value="02" />
		</antcall>
		<antcall target="performTest">
			<param name="test.id" value="03" />
		</antcall>
		<antcall target="performTest">
			<param name="test.id" value="04" />
		</antcall>
		<antcall target="performTest">
			<param name="test.id" value="05" />
		</antcall>
		<antcall target="performDbTest">
			<param name="test.id" value="06" />
		</antcall>
		<antcall target="performTest">
			<param name="test.id" value="07" />
		</antcall>
		-->
	</target>
	<!--
	<target name="performTest">
		<antcall target="remove" />
		<exec executable="${catalina.home}/bin/shutdown.sh">
			<env key="JAVA_HOME" value="${java.home}" />
		</exec>
		<sleep milliseconds="1000" />
		<exec executable="${catalina.home}/bin/startup.sh">
			<env key="JAVA_HOME" value="${java.home}" />
			<env key="CATALINA_OPTS" value="-server" />
				<env key="CATALINA_OPTS" value="-server -Xmx32M -Dcom.sun.management.jmxremote -Xdebug -Xrunjdwp:transport=dt_socket,address=8000,server=y,suspend=n" /> 
	
		</exec>
		<waitfor>
			<and>
				<http url="http://localhost:8080/" />
			</and>
		</waitfor>
		<exec executable="dropdb">
			<env key="PGPORT" value="30103" />
			<arg line="-p 30103" />
			<arg value="chellow" />
		</exec>

		<exec executable="createdb" failonerror="true">
			<env key="PGPORT" value="30103" />

			<arg line="-p 30103 -T template0" />

			<arg value="chellow" />
		</exec>

		<antcall target="install" />
		<echo message="Doing test ${test.id}" />
		<java jar="${imprimatur.jar}" fork="true" failonerror="true">
			<arg value="${test.home}/${test.id}/imprimatur.xml" />
		</java>
	</target>
-->

	<!--
	<target name="performDbTest">
		<antcall target="remove" />
		<exec executable="${catalina.home}/bin/shutdown.sh">
			<env key="JAVA_HOME" value="${java.home}" />
		</exec>
		<sleep milliseconds="1000" />
		<exec executable="${catalina.home}/bin/startup.sh">
			<env key="JAVA_HOME" value="${java.home}" />
			<env key="CATALINA_OPTS" value="-server -Xmx32M -Dcom.sun.management.jmxremote -Xdebug -Xrunjdwp:transport=dt_socket,address=8000,server=y,suspend=n" />
		</exec>
		<waitfor>
			<and>
				<http url="http://localhost:8080/" />
			</and>
		</waitfor>
		<exec executable="dropdb">
			<env key="PGPORT" value="30103" />
			<arg line="-p 30103" />
			<arg value="chellow" />
		</exec>

		<exec executable="createdb" failonerror="true">
			<env key="PGPORT" value="30103" />

			<arg line="-p 30103 -T template0" />

			<arg value="chellow" />
		</exec>

		<exec executable="psql" failonerror="true">
			<env key="PGPORT" value="30103" />

			<arg line="chellow -p 30103 -q file ${test.home}/${test.id}/chellow.pgdump" />
		</exec>

		<antcall target="install" />
		<echo message="Doing test ${test.id}" />
		<java jar="${imprimatur.jar}" fork="true" failonerror="true">
			<arg value="${test.home}/${test.id}/imprimatur.xml" />
		</java>
	</target>
	-->
	<!--
	<target name="oldTest" depends="compile" description="Tests the application">
		<exec executable="dropdb">
			<env key="PGPORT" value="30103" />
			<arg line="-p 30103" />
			<arg value="chellow" />
		</exec>

		<exec executable="createdb" failonerror="true">
			<env key="PGPORT" value="30103" />

			<arg line="-p 30103 -T template0" />

			<arg value="chellow" />
		</exec>

		<antcall target="install" />

		<java jar="${imprimatur.jar}" fork="true" failonerror="true">
			<arg value="${test.home}/imprimatur.xml" />
		</java>

	</target>
	-->
	<!-- ==================== Javadoc Target ================================== -->

	<!--

  The "javadoc" target creates Javadoc API documentation for the Java
  classes included in your application.  Normally, this is only required
  when preparing a distribution release, but is available as a separate
  target in case the developer wants to create Javadocs independently.

-->

	<target name="javadoc" depends="compile" description="Create Javadoc API documentation">

		<mkdir dir="${dist.home}/docs/api" />
		<javadoc sourcepath="${src.home}" destdir="${dist.home}/docs/api" packagenames="*">
			<classpath refid="compile.classpath" />
		</javadoc>

	</target>



	<!-- ====================== List Target =================================== -->

	<!--

  The "list" target asks the specified Tomcat 5 installation to list the
  currently running web applications, either loaded at startup time or
  installed dynamically.  It is useful to determine whether or not the
  application you are currently developing has been installed.

-->

	<target name="list" description="List installed applications on servlet container">

		<list url="${manager.url}" username="${manager.username}" password="${manager.password}" />

	</target>

	<target name="short-test" description="Short Test">
		<property name="mdd.dir" value="mdd-test" />
		<antcall target="compile" />
		<antcall target="reset-db" />
		<antcall target="install" />
		<copy file="${test.home}/imprimatur.pre.xml" tofile="${test.home}/imprimatur.xml" />
		<replace file="${test.home}/imprimatur.xml" token="@@test.port@@" value="${test.port}" />
		<java jar="${imprimatur.jar}" fork="true" failonerror="true">
			<arg value="${test.home}/imprimatur.xml" />
		</java>
	</target>

	<!-- ==================== Prepare Target ================================== -->

	<!--

  The "prepare" target is used to create the "build" destination directory,
  and copy the static contents of your web application to it.  If you need
  to copy static files from external dependencies, you can customize the
  contents of this task.

  Normally, this task is executed indirectly when needed.

-->

	<target name="prepare">

		<!-- Create build directories as needed -->
		<mkdir dir="${build.home}" />
		<mkdir dir="${build.home}/WEB-INF" />
		<mkdir dir="${build.home}/WEB-INF/classes" />

		<condition property="mdd.dir" value="mdd">
			<not>
				<isset property="mdd.dir" />
			</not>
		</condition>
		<echo message="${mdd.dir}" />
		<!-- Copy static content of this web application -->
		<copy todir="${build.home}">
			<fileset dir="${web.home}" excludes="WEB-INF/mdd/** WEB-INF/mdd-test/**" />
		</copy>

		<copy todir="${build.home}/WEB-INF/mdd">
			<fileset dir="${web.home}/WEB-INF/${mdd.dir}" />
		</copy>
		<!--
		<exec dir="${basedir}" executable="svnversion" output="${build.home}/WEB-INF/VERSION" />
		-->
		<echo message="348" file="${build.home}/WEB-INF/VERSION" />
				

		<!-- Copy external dependencies as required -->
		<!-- *** CUSTOMIZE HERE AS REQUIRED BY YOUR APPLICATION *** -->
		<mkdir dir="${build.home}/WEB-INF/lib" />
		<!--
    <copy todir="${build.home}/WEB-INF/lib" file="${foo.jar}"/>
-->

		<!-- Copy static files from external dependencies as needed -->
		<!-- *** CUSTOMIZE HERE AS REQUIRED BY YOUR APPLICATION *** -->
		<!--
-->
		<!--
		<copy todir="${build.home}/WEB-INF/lib" file="/usr/share/java/commons-dbcp-1.2.1.jar"/>
		-->
		<copy todir="${build.home}/WEB-INF/lib" file="${javalib.home}/slf4j-1.5.8/slf4j-jdk14-1.5.8.jar" />
		<copy todir="${build.home}/WEB-INF/lib" file="${javalib.home}/javamail-1.4.2/mail.jar" />

		<copy todir="${build.home}/WEB-INF/lib" file="${javalib.home}/hibernate-distribution-3.3.2.GA/hibernate3.jar">
			<fileset dir="${javalib.home}/hibernate-distribution-3.3.2.GA/lib/required" />
		</copy>
		<copy todir="${build.home}/WEB-INF/lib" file="${javalib.home}/commons-fileupload-1.2.1/lib/commons-fileupload-1.2.1.jar" />
		<copy todir="${build.home}/WEB-INF/lib" file="${javalib.home}/commons-io-1.4/commons-io-1.4.jar" />

		<copy todir="${build.home}/WEB-INF/lib" file="${javalib.home}/ostermillerutils_1_07_00.jar" />

		<copy todir="${build.home}/WEB-INF/lib" file="${javalib.home}/commons-net-2.0/commons-net-2.0.jar" />
		<copy todir="${build.home}/WEB-INF/lib" file="${javalib.home}/jakarta-oro-2.0.8/jakarta-oro-2.0.8.jar" />
		<copy todir="${build.home}/WEB-INF/lib" file="${javalib.home}/commons-httpclient-3.1/commons-httpclient-3.1.jar" />
		<copy todir="${build.home}/WEB-INF/lib" file="${javalib.home}/commons-codec-1.4/commons-codec-1.4.jar" />
		<copy todir="${build.home}/WEB-INF/lib" file="${javalib.home}/jython-2.5.1/jython.jar" />
		<!--
		<copy todir="${build.home}/WEB-INF/lib" file="${javalib.home}/jsr223-engines/jython/build/jython-engine.jar" />
		<copy todir="${build.home}/WEB-INF/lib" file="${javalib.home}/batik-1.6/lib/batik-transcoder.jar" />
		<copy todir="${build.home}/WEB-INF/lib" file="${javalib.home}/batik-1.6/lib/batik-dom.jar" />
		<copy todir="${build.home}/WEB-INF/lib" file="${javalib.home}/batik-1.6/lib/batik-bridge.jar" />
		<copy todir="${build.home}/WEB-INF/lib" file="${javalib.home}/batik-1.6/lib/batik-gvt.jar" />
		<copy todir="${build.home}/WEB-INF/lib" file="${javalib.home}/batik-1.6/lib/batik-svg-dom.jar" />		
		<copy todir="${build.home}/WEB-INF/lib" file="${javalib.home}/batik-1.6/lib/batik-util.jar" />		
		<copy todir="${build.home}/WEB-INF/lib" file="${javalib.home}/batik-1.6/lib/batik-css.jar" />		
		<copy todir="${build.home}/WEB-INF/lib" file="${javalib.home}/batik-1.6/lib/xerces_2_5_0.jar" />
		<copy todir="${build.home}/WEB-INF/lib" file="${javalib.home}/batik-1.6/lib/batik-ext.jar" />	
		<copy todir="${build.home}/WEB-INF/lib" file="${javalib.home}/batik-1.6/lib/batik-xml.jar" />	
		<copy todir="${build.home}/WEB-INF/lib" file="${javalib.home}/batik-1.6/lib/batik-script.jar" />
		<copy todir="${build.home}/WEB-INF/lib" file="${javalib.home}/batik-1.6/lib/batik-awt-util.jar" />
		<copy todir="${build.home}/WEB-INF/lib" file="${javalib.home}/batik-1.6/lib/batik-parser.jar" />
		-->

		<!--
		<copy todir="${build.home}/WEB-INF/lib" file="/usr/share/java/xalan2.jar" />
		<copy todir="${build.home}/WEB-INF/lib" file="/usr/share/java/serializer.jar" />
		-->
	</target>

	<!-- ==================== Reload Target =================================== -->

	<!--

  The "reload" signals the specified application Tomcat 5 to shut itself down
  and reload. This can be useful when the web application context is not
  reloadable and you have updated classes or property files in the
  /WEB-INF/classes directory or when you have added or updated jar files in the
  /WEB-INF/lib directory.

  NOTE: The /WEB-INF/web.xml web application configuration file is not reread
  on a reload. If you have made changes to your web.xml file you must stop
  then start the web application. 

-->

	<target name="reload" depends="compile" description="Reload application on servlet container">

		<reload url="${manager.url}" username="${manager.username}" password="${manager.password}" path="${app.path}" />

	</target>


	<!-- ==================== Remove Target =================================== -->

	<!--

  The "remove" target tells the specified Tomcat 5 installation to dynamically
  remove this web application from service.

  NOTE:  This is the logical opposite of the "install" target.

-->

	<target name="remove" description="Remove application on servlet container">

		<undeploy url="${manager.url}" username="${manager.username}" password="${manager.password}" path="${app.path}" />

	</target>
</project>
